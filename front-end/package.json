{
  "name": "@airChain/monorepo",
  "version": "1.0.0",
  "keywords": [
    "ethereum",
    "react",
    "workspaces",
    "yarn"
  ],
  "private": true,
  "scripts": {
    "react-app:build": "yarn workspace @airChain/react-app build",
    "react-app:eject": "yarn workspace @airChain/react-app eject",
    "react-app:start": "yarn workspace @airChain/react-app start",
    "react-app:test": "yarn workspace @airChain/react-app test",
    "subgraph:auth": "yarn workspace @airChain/subgraph auth",
    "subgraph:codegen": "yarn workspace @airChain/subgraph codegen",
    "subgraph:build": "yarn workspace @airChain/subgraph build",
    "subgraph:deploy": "yarn workspace @airChain/subgraph deploy"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/@graphprotocol/graph-ts",
      "**/@graphprotocol/graph-ts/**"
    ]
  }
}
